/**
 * Editor Setup Constants
 * 编辑器配置常量
 */

/**
 * 默认编辑器预设
 */
export const DEFAULT_PRESET = 'full'

/**
 * 默认是否启用通用 ID
 */
export const DEFAULT_ENABLE_UNIVERSAL_ID = true

/**
 * 默认编辑器属性配置
 */
export const DEFAULT_EDITOR_PROPS = {
  attributes: {
    class: 'focus:outline-none min-h-[500px] max-w-4xl mx-auto p-6',
  },
}
